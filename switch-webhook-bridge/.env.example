# Port for the HTTP bridge server
PORT=3001

# HTTPS endpoint to forward webhooks to
FORWARD_TO_URL=https://sipnplay.cafe/api/switch-webhook

# CORS allowed origins (comma-separated, * for all)
ALLOWED_ORIGINS=*

# Ignore SSL certificate errors (only for development!)
IGNORE_SSL=false